# Build-time config for `vite build --mode staging` (see mentoring.Dockerfile).
# Only VITE_-prefixed variables are exposed to the bundle, and they are inlined
# at build time: never put a secret here.
VITE_API_URL=https://public-api.stage.adikteev.com
